# from functools import reduce

# scores = [75, 65, 80, 95, 50]

# total = reduce(lambda a, b: a + b, scores)

# print(total)


def pru ():
    preg = int (input ('numero 1-5: '))
    var = [1,2,3,4,5]

    for i in var:
        if i == preg:
            print ('Numero dentro de la lista (1-5)')
    # else:
    #     print ('NO está dentro de la lista (1-5)')

pru()

